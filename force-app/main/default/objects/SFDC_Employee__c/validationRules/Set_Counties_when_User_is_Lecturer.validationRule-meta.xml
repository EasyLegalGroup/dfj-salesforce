<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Set_Counties_when_User_is_Lecturer</fullName>
    <active>true</active>
    <description>If user is &quot;Lecturer&quot;, you must set the Counties in which the user can host lectures.</description>
    <errorConditionFormula>AND(
 SF_User__r.IsLecturer__c  = TRUE,
ISBLANK(Counties__c)
)</errorConditionFormula>
    <errorMessage>If user is &quot;Lecturer&quot;, you must set the Counties in which the user can host lectures.</errorMessage>
</ValidationRule>
