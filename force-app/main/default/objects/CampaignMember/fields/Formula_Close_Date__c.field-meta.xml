<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Formula_Close_Date__c</fullName>
    <formula>IF(
    NOT(ISBLANK(Order_Created_Date_Time__c)), 
    Order_Created_Date_Time__c, 
    IF(
        NOT(ISBLANK(Lead_Closed_Date_Time__c)), 
        Lead_Closed_Date_Time__c, 
        NULL
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Formula Close Date</label>
    <required>false</required>
    <type>DateTime</type>
</CustomField>
