<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Days_to_Close__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    NOT(ISBLANK(Order_Created_Date_Time__c)),
    (Order_Created_Date_Time__c - Campaign.Start__c) * 24 / 24,
    IF(
        NOT(ISBLANK(Lead_Closed_Date_Time__c)),
        (Lead_Closed_Date_Time__c - Campaign.Start__c) * 24 / 24,
        NULL
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Days to Close</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
